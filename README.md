This main branch is pretty vanilla. Consider one of these branches:
- `git checkout convex`
- ...more to come?

# How-to

- Lint:

    ```zsh
    npm run lint
    ```

- Run dev:

    ```zsh
    npm run dev
    # and in another window,
    npx convex dev
    ```

- Test:

    ```zsh
    npm run test
    ```
